const { User, Thought } = require('../models');

// get all thoughts
// et single thought by id
// post new thought and push created through id to associated user thoughts array field
//put update thought by id
// delete remove thought by id

// /api/thoughts/:thoughtId/reactions\
// post to create reaction stored in a single thought reactions array field
// delete to pull and remove reaction by reactionId value